---
description: Never commit secrets_local.py (contains Dhan API credentials)
alwaysApply: true
---

Hard rule:
- Never include `secrets_local.py` in any `git commit` (or staging action).

If you need keys for running locally:
- Read them from `secrets_local.py`.
- Do not copy secrets into other committed files.

